<% provide(:title, 'All members') %>

<h1>All members</h1>

<%= will_paginate %>

<ul class="members">
  <% @members.each do |member| %>
      <li>
        <%= link_to member.name, member %>
      </li>
  <% end %>
</ul>

<%= will_paginate %>